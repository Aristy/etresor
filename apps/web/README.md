# eTresor — Web (Next.js recommandé)

## Scaffold suggéré
```bash
npx create-next-app@latest web --ts --eslint --src-dir --app --tailwind --import-alias "@/*"
```

## TODO
- Auth (OIDC/Keycloak)
- Pages: Accueil, Services, Dossiers, Paiements, Quittances
- Dashboard agents/admin